# Storyline

前后端分离可编辑时间线系统：
- 前端：Vue3 + TS + Vite + Pinia + Tailwind + Canvas 渲染
- 后端：NestJS + GraphQL（可接 PostgreSQL）

## 目录
- `frontend/` 交互时间轴客户端
- `backend/` GraphQL 服务端

## 运行
```bash
cd frontend && npm i && npm run dev
cd backend && npm i && npm run start:dev
```
